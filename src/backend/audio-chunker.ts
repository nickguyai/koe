// Re-export shim — canonical location is now src/audio/audio-chunker.ts
export { splitAudio, cleanupChunks, getAudioDuration, isLongAudio, computeChunkRanges } from '../audio/audio-chunker';
export type { AudioChunk } from '../audio/audio-chunker';
