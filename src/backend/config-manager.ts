// Re-export shim — canonical location is now src/config/config-manager.ts
export { ConfigManager } from '../config/config-manager';
export type { HotkeySettings, AppSettings } from '../config/config-manager';
