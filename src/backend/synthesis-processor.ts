// Re-export shim — canonical location is now src/transcription/synthesis-processor.ts
export { SynthesisProcessor } from '../transcription/synthesis-processor';
export type { ConsensusMetadata, ConsensusTranscriptionResult } from '../transcription/synthesis-processor';
