// Re-export shim — canonical location is now src/ui/recording-widget.ts
export { RecordingWidget, getRecordingWidget, initRecordingWidget } from './ui/recording-widget';
export type { WidgetState } from './ui/recording-widget';
